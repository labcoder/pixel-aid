{
  "name": "@pixelaid/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "benchmark": "vitest bench --run src/fixtureSuite.bench.ts src/singleSpriteCleanup.bench.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@pixelaid/shared": "0.1.0"
  },
  "devDependencies": {
    "@pixelaid/fixtures": "0.1.0"
  }
}
