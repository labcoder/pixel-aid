{
  "name": "@pixelaid/core",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "benchmark": "vitest bench --run src/fixtureSuite.bench.ts src/singleSpriteCleanup.bench.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:visual": "vitest run src/visualRegression.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@pixelaid/shared": "0.1.1"
  },
  "devDependencies": {
    "@jsquash/webp": "^1.5.0",
    "@pixelaid/fixtures": "0.1.1"
  }
}
